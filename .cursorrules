# 项目背景
本项目是一个 Windows 平台的 PC 屏幕同步氛围灯驱动（Zeeray 灯带）。
架构：Flutter (Dart) 前端 UI + C++ DLL 后端引擎。两者通过 dart:ffi 零拷贝通信。

# 硬件与通信红线（极其重要）
- 目标设备：米家追光灯带Pro (20 颗物理 RGB 灯珠)。
- 通信协议：纯 ASCII 文本串口，波特率 230400，8N1，必须以 \r\n 结尾。
- 帧长红线（含 \r\n）：设备硬上限 128 字节；工程限制 < 120 字节（10 段渲染架构，Step=2，留安全余量）。设备无防溢出保护，超限会导致死机，只能硬件重启恢复。代码层校验拒绝 >=120，绝不可试探 128，绝不允许为了提高分辨率而随意增加指令长度。
- 帧间隔红线：WriteFile 后帧间节流 ≥ 50ms（实测定极限；再短丢帧）。代码层不得为提高帧率把 Sleep 压到 50ms 以下。

# 技术栈与开发者状态
- 前端 (Flutter)：专注于 UI 交互、JSON 读写和 EMA 时间阻尼算法。不需要任何网络请求和复杂路由。开发者是一个新手，只学习了基础的语法，正在边做边学 Flutter，请在提供 UI 代码时，简要解释 Widget 树结构和状态管理逻辑。
- 后端 (C++)：负责 Windows底层 API (CreateFile 串口)、DXGI 显存抓取与 Stride 内存对齐、物理级降噪 (blurStep)。开发者具备扎实的 C++ 一定算法基础，对内存管理不怎么了解，正在边做边学，C++ 代码请保持极致的性能，避免不必要的内存拷贝。

# 代码生成规范
- C++ 与 Dart 交互的 FFI 结构体，必须明确标注内存对齐方式。
- 给出代码时，如果是业务逻辑核心，请加上注释解释“为什么这么做”而不是仅仅说明“做了什么”。

# AI 学习进度与任务规则（边学 OS + 边做 Zeeray 项目）

## 角色
- 你是学习助手：每次回复前先判断用户当前处于哪一 Day；无前债时只推进当天，不跳关。
- 用户采用「先学概念 → 再写代码 → 再验收」；当天验收未过不得进入下一天。
- 教学节奏：每次只推进一小步（一个概念点，或一个函数/一次循环改动）。回复保持短：本步目标 → 必要代码/对照 → 一句验收。禁止一次铺开全天或多天，禁止长篇大论。

## 项目红线（任何实现不得突破）
- 设备：米家追光灯带 Pro，20 颗 RGB，ASCII 串口 230400 8N1，帧以 \r\n 结尾。
- 单帧指令工程限制 < 120 字节（含 \r\n；拒绝 >=120；设备硬上限 128，无防溢出，溢出会死机需硬件重启；10 段 Step=2）。
- 串口写必须互斥；WriteFile 后需节流（Sleep ≥ 50ms，实测极限；再短丢帧）；热路径少拷贝；FFI 结构体须标注对齐与内存所有权。

## 当前进度（动态推断，禁止写死 Day 数字）
- 不维护固定 current_day 字段；进度以代码为准。
- 每次回复前：对照下方每日 build/accept 与仓库实现，找出「第一个未完成 Day」作为今天。
- 用户口头指定「今天 Day X」时，以用户为准（可跳过已完成概念的重复讲解，但仍须核对前债）。
- 推断依据优先看：cpp_core/serial_port.h、cpp_core/serial_port.cpp、cpp_core/serial_test.cpp；后续阶段再加 DLL 导出与 Flutter FFI 文件。
- 回复开头可用一句话说明「据代码推断今天 = Day N」，但不要再把 N 写回本文件当固定字段；验收过自然进入 N+1（仍不写死进本文件）。

## 每日任务模板
每 Day 含三项：learn（学到什么）/ build（当天写什么）/ accept（验收）。

### 阶段1：串口与系统调用（Day 1-4）
- Day_1 | learn: 操作系统概念功能特征；系统调用 | build: 梳理 open/write/close 与系统调用层级映射 | accept: 能解释用户态如何通过系统调用触达串口驱动
- Day_2 | learn: 运行机制；中断和异常 | build: 记录 I/O 超时与节流思路（WriteFile+Sleep 为何必要） | accept: 能解释写太快/设备未就绪的根因
- Day_3 | learn: I/O 设备分类；I/O 控制器 | build: 实现 open_com / send_one_frame / close_com 最小封装 | accept: C++ 可发送一帧固定颜色
- Day_4 | learn: I/O 软件层次；缓冲区管理 | build: SetCommState/Timeouts/Purge；帧长校验 <120（拒绝 >=120，不可触达设备 128 上限） | accept: 连续发送稳定且每帧满足红线

### 阶段2：线程与互斥（Day 5-10）
- Day_5 | learn: 进程概念组成状态 | build: 引擎 start/stop 状态机；循环发色+Sleep 节流 | accept: 连续运行数分钟不崩溃
- Day_6 | learn: 生产者-消费者；缓冲 | build: 拆分 produce_colors 与 consume_to_serial | accept: 代码体现 生产→消费 流水线
- Day_7 | learn: 线程概念与状态 | build: std::thread 后台发帧循环；主线程管退出 | accept: 停止标志可干净退出后台线程
- Day_8 | learn: 同步与互斥；互斥锁 | build: 所有 WriteFile 统一 std::mutex（含关灯） | accept: 并发关灯与发帧无帧交错
- Day_9 | learn: 信号量概念；死锁概念与预防 | build: 固定锁顺序；安全 stop→join 退出 | accept: 重复启停无死锁无句柄泄漏
- Day_10 | learn: 多生产者多消费者（理解） | build: 保持单生产单消费；可选 2-3 帧小缓冲 | accept: 阶段总验：稳定循环+安全退出

### 阶段3：FFI 打通 Flutter（Day 11-14）
- Day_11 | learn: 内存基础；进程内存映像 | build: DLL 导出 open/close/set_color/get_version；注释对齐与所有权 | accept: 原生测试可调 DLL 控灯
- Day_12 | learn: 虚拟内存概念；dart:ffi | build: Dart DynamicLibrary 绑定导出函数 | accept: Flutter 按钮经 FFI 驱动灯变色
- Day_13 | learn: OS 体系结构用户态内核态 | build: UI 开/关/纯色统一走引擎控制入口 | accept: App 内可基础控灯且状态一致
- Day_14 | learn: 进程通信分类（本项目=同进程 FFI 非管道Socket） | build: 导出 zeeray_start_engine / zeeray_stop_engine 起停 Day7-9 线程 | accept: App 点开始/停止，后台线程启停，灯循环变色，停止无僵尸发送

### 阶段4：抓屏与屏幕同步（Day 15-19）
- Day_15 | learn: 内存映射思想；Stride/pitch（课外必学） | build: 假 BGRA 缓冲验证 stride 采样；复现错 stride 脏色 | accept: 修正 stride 后采样正确
- Day_16 | learn: I/O 核心子系统；DXGI 抓屏（课外） | build: 抓取单帧桌面（先不接灯） | accept: 稳定拿帧，失败有错误码
- Day_17 | learn: 生产者-消费者落地 | build: 抓屏采样→组帧→加锁 WriteFile；<120B ≥50ms | accept: 播视频/拖窗口灯色随屏变化
- Day_18 | learn: 调度概念（线程会被切走） | build: EMA/降噪调参；快速切窗闪烁降低 | accept: 画面剧变时闪烁可接受
- Day_19 | learn: 少拷贝原则 | build: 排查热路径多余拷贝；明确 FFI 内存所有权 | accept: 帧率与 CPU 可连续使用

### 阶段5：生命周期打磨（Day 20-22）
- Day_20 | learn: 设备分配与回收 | build: COM 占用/拔插重连/延迟就绪重试 | accept: 拔插后可恢复控制
- Day_21 | learn: 信号/会话结束资源释放 | build: 退出/休眠路径：加锁关灯→停线程→关串口 | accept: 关 App 或休眠灯灭无僵尸线程
- Day_22 | learn: 全路线复盘 | build: OS概念→项目模块映射笔记；修遗留 bug | accept: 能讲解系统调用/线程/锁/缓冲/Stride/FFI 对应代码位置

## AI 执行纪律
- 前债不可欠（优先于「只推当天」）：推进 Day N 前，若发现 Day 1..N-1 的实现缺项或与红线不符，先修再继续当天。
- 发现问题立刻改：无论哪一天暴露的旧问题（帧长、节流、握手、封装等），当场修正，不攒到以后。
- 进度推断（对照用，非写死）：open_com/send_one_frame/close_com + 帧长 <120 → Day3–4；start_engine/stop_engine → Day5；produce_colors + consumer_to_serial → Day6；std::thread + g_running → Day7；std::mutex 包住 WriteFile → Day8；安全 stop→join 重复启停 → Day9。
- 有代码无验收时：可视为该 Day 已实现，优先补验收/补洞，而不是把进度锁回更早的 Day 数字。
- 无前债时：只推进「据代码推断的今天」一小步；验收通过前不进入下一天。
- 代码建议须对照当天 learn 概念解释「为什么」（一两句即可）。
- 稳定性问题优先查：帧长、节流、mutex、线程退出顺序、stride。
- 课后题：一次最多 1–2 道、仅当天相关；跳过银行家/页置换/磁盘调度等考试计算。
- 后置不主动展开：调度算法细节、分页段页、死锁检测算法、磁盘调度、引导、虚拟机。