# Zeeray Ambilight — 项目框架与推进规则

> 本文件是本项目的**唯一权威**（架构 + 红线 + 计划 + 协作纪律）。
> `docs/AI_STATUS.md`、`docs/PROJECT_STATUS.md` 为详细背景参考，不再与本文件双向同步；
> 旧的 Day 1-25 教学日程已归档在 `backup/cursorrules`，**不再作为进度依据**。

---

## 1. 项目与架构

Windows 平台 PC 屏幕同步氛围灯驱动（米家追光灯带 Pro / Zeeray，20 颗物理 RGB 灯珠）。

**架构 v2 — 双进程：**

| 进程 | 职责 | 禁止 |
|------|------|------|
| Flutter (Dart) | UI 交互、模式切换、JSON 配置、参数展示与编辑 | 直接开串口、传逐帧像素、走发帧热路径 |
| `helper.exe` (C++) | 串口唯一属主、DXGI 抓屏、stride 采样、组 ASCII 帧、发帧循环、IPC 服务 | 对外监听、接受多连接、让 Flutter 碰 COM |

两进程只经本机 IPC（`127.0.0.1:9527` 回环 TCP）传**命令 / 状态文本**；逐帧像素与发帧热路径全部留在 helper 内，**不再经 `dart:ffi`**。

```mermaid
flowchart LR
  subgraph flutter [Flutter_Process]
    UI[Pages_material3]
    State[Riverpod_Notifiers]
    Cfg[JSON_Config]
    Client[HelperClient]
  end

  subgraph helper [helper_exe]
    IPC[ipc_loop_9527]
    Engine[light_engine]
    DXGI[dxgi_capture]
    Serial[serial_port]
  end

  Device[Zeeray_LED_Strip]

  UI --> State
  Cfg --> State
  State --> Client
  Client -->|"commands_and_status_text"| IPC
  IPC --> Engine
  Engine --> DXGI
  Engine --> Serial
  Serial -->|"ASCII_lt_120B"| Device
```

**历史废案（勿回改主线）**：早期是「Flutter + C++ DLL 同进程 + `dart:ffi`」，已验证可控灯，但发帧线程与 Flutter 渲染同进程抢资源，窗口可见时丢帧，故拆为双进程。学习产物 `cpp_core/废案_同进程FFI_Day11-14/`、`lib - 副本/` 保留但**不是主线**，不要从里面复制 FFI 代码。

---

## 2. 硬红线（任何实现不得突破）

**设备与协议**

- 米家追光灯带 Pro，20 颗物理 RGB；10 段渲染、每段 `Step=2`。
- 纯 ASCII 文本串口，**230400 8N1**，每帧必须以 `\r\n` 结尾。

**帧长**

- 含 `\r\n` 工程限制 **< 120 字节**；代码层拒绝 `>= 120`（见 `serial_port.cpp` 的 `send_one_frame` 与 `light_engine.cpp` 组帧后的二次校验）。
- 设备硬上限 128 且**无防溢出保护**，超限会死机、只能硬件重启。**绝不试探 128**，绝不为提高分辨率而加长指令。

**帧间隔**

- `WriteFile` 之后必须 `Sleep(50)` 以上（实测极限，再短丢帧）。**不得为提高帧率压到 50ms 以下。**

**并发**

- 所有串口写（含关灯 / 握手路径）统一走 `send_one_frame` 的 `std::mutex`。
- 引擎停止顺序：置停止标志 → 关灯 → `join` 线程 → 关串口。

**进程边界**

- 串口唯一属主 = `helper.exe`；Flutter 绝不直接开串口（防两进程抢 COM）。
- IPC 绑定 `127.0.0.1`、只 accept **单连接**，不对外监听；只传命令/状态，**绝不传逐帧像素**。
- helper 必须随 App 退出而干净退出：关灯 → 停线程 → 关串口 → 关 DXGI → 进程退出，防僵尸进程占用 COM。

**配置边界（阶段 B 扩协议时同样是红线）**

- **帧长上限与 50ms 节流不得作为可配置项暴露给 Flutter。** 可下发的只有 EMA α、模式、COM 口名、blurStep、采样参数等；任何来自 IPC 的数值都要在 helper 侧夹紧（clamp）后再用。

---

## 3. 代码框架

### 3.1 helper（`cpp_core/`，已基本就绪）

| 文件 | 职责 |
|------|------|
| `helper_main.cpp` | 入口：DXGI 初始化 → 串口就绪重试 → 电源监听 → `ipc_run` 阻塞 → `helper_shutdown` |
| `helper_lifecycle.h` | 退出序列声明（`helper_shutdown` 多路径防重入） |
| `ipc_loop.cpp` | Winsock 监听 / accept / 按行读 / `dispatch_line` / `send_status` / `ipc_cancel` |
| `light_engine.cpp` | 握手、纯色、`produce_colors`（DXGI 采样 → EMA → 组帧）、`consumer_to_serial`、`frame_loop`、start/stop |
| `dxgi_capture.cpp` | Desktop Duplication、stride 采样、10 段边缘、blurStep 水平平均 |
| `serial_port.cpp` | `open_com` / `send_one_frame`（帧长校验 + 互斥）/ `read_response_ok` / `close_com` |
| `power_watch.cpp` | 隐藏窗拦截休眠 / 会话结束消息 |

### 3.2 Flutter（`lib/`，目标结构，当前只有单文件烟测台）

```text
lib/
├── main.dart                  # runApp(ProviderScope(child: ZeerayApp()))
├── app/
│   ├── app.dart               # MaterialApp + NavigationRail 壳
│   └── theme.dart             # 主题（可后置）
├── ui/
│   ├── pages/
│   │   ├── control_page.dart  # 主控：连接 / 开始 / 停止 / 纯色 / 关灯 + 状态
│   │   └── settings_page.dart # 设置：EMA α、方案 A/B、COM 口
│   └── widgets/               # 状态徽标、色卡按钮等小组件
├── state/
│   ├── helper_state.dart      # 连接与引擎状态 Notifier + Provider
│   └── config_state.dart      # 配置 Notifier（读写 JSON）
├── ipc/
│   ├── helper_client.dart     # Process.start + Socket + 按行收发（从 main.dart 搬）
│   └── helper_status.dart     # status 行 → enum 解析
└── config/
    ├── app_config.dart        # 数据类 + fromJson / toJson
    └── config_store.dart      # exe 同目录 JSON 读写
```

**待清理**：`lib/engine/`、`lib/serial/` 是 FFI 时代遗留的空目录，与「Flutter 不碰串口」边界冲突，A0 时删除，避免误导后续实现。

---

## 4. 前端技术栈约定

**UI：Material 3（Flutter 内置）**

- 用 `MaterialApp` + `ThemeData(colorScheme: ColorScheme.fromSeed(...))`，浅色/深色各一套、跟随系统；当前 Flutter 版本 Material 3 已是默认，不必再写 `useMaterial3: true`。
- 桌面壳用 `NavigationRail`（主控 / 设置），页面主体用 `Scaffold`。**不要混用 `fluent_ui` 做壳**；`pubspec.yaml` 里的 `fluent_ui` 依赖在切换完成后移除。
- 选 Material 3 的理由：生态与第三方扩展最全，配色、动效、组件库都能直接复用。
- 扩展包按需引入，但**克制**：确有需要再加（如窗口/托盘用 `window_manager`、主题用 `flex_color_scheme`、转场用 `animations`），加之前先说明为什么内置能力不够。

**状态管理：`flutter_riverpod ^3.4.1`**

- 用 `Notifier` + `NotifierProvider`（Riverpod 3 的推荐写法）。**`StateNotifier` / `StateProvider` 已是 legacy，禁止生成。**
- 页面用 `ConsumerWidget` / `ConsumerStatefulWidget`；`build` 里 `ref.watch(...)` 订阅，回调里 `ref.read(...).方法()` 触发动作。
- 副作用（`Process.start`、`Socket`、文件读写）只允许出现在 Notifier 方法或 `ref.onDispose` 中，**绝不写在 `build` 里**。
- `HelperClient` 由 `Provider` 暴露单例，并在 `ref.onDispose` 里发 `quit` 后 `destroy` socket。

**配置持久化：手写 JSON**（`dart:io` + `dart:convert`，不引第三方存储包）

- 配置文件放 **exe 同目录**：`File(Platform.resolvedExecutable).parent` 下的 `zeeray_config.json`。
- 读失败 / 解析失败 → 回退默认值并继续启动，不抛给用户。

**路径解析规则（helper 与配置同用）**

- 先找 exe 同目录的 `helper.exe`；找不到再回退开发路径 `cpp_core/build/Release/helper.exe`。
- **禁止硬编码绝对路径**。当前 `lib/main.dart` 里的 `D:\Project\...\helper.exe` 属于待清理项（A1 修）。

**前端职责边界**

| 做 | 不做 |
|----|------|
| UI 交互、模式切换、JSON 读写、参数展示与编辑、状态呈现 | 网络请求、复杂路由、直接开串口、逐帧像素热路径、在 Dart 侧做 EMA/组帧 |

**若将来重新引入 `dart:ffi`**（当前无）：FFI 结构体必须标注内存对齐方式与内存所有权。

---

## 5. IPC 协议

一行一条命令，`\n` 结尾。未知命令 helper 打日志并忽略、不崩；超长行（>200 字符）丢弃到行尾。

**helper 当前已支持**（对齐 `ipc_loop.cpp` 的 `dispatch_line`）：

| 方向 | 文本 | 含义 |
|------|------|------|
| → helper | `start` | 启动氛围灯引擎线程 |
| → helper | `stop` | 停止引擎（join 线程） |
| → helper | `solid RRGGBB` | 发一帧纯色（6 位 hex，非法值忽略） |
| → helper | `off` | 停引擎并关灯（`set_power 0`） |
| → helper | `quit` | 停引擎并结束 IPC 循环，helper 退出 |
| → helper | `reconnect` | 关串口并重试就绪（最多 10 次） |
| ← helper | `status ready` | 客户端接通、串口已就绪 |
| ← helper | `status reconnecting` / `reconnect_ok` / `reconnect_fail` | 重连过程与结果 |

**尚未实现**（阶段 B 落地）：`set alpha <0..1>`、`set mode a|b`、`set com <COMn>`、`set blur <n>`，以及更宽的状态上报。协议扩展先写清草稿（命令名 / 取值域 / 越界行为），再动 C++。

---

## 6. 推进计划

**用法**：从上往下取**第一项未勾选**的做；一次只推一小步，验收通过再进下一项。已掌握的概念可跳过讲解，但不跳过验收。

### 阶段 A — 前端产品壳 + 真 IPC（当前焦点）

现有 `lib/main.dart` 的 IPC 能力（拉起 helper、Socket 收发、唤醒重连）**必须原样迁移、不得倒退成 mock**。

- [ ] **A0 起壳**：`flutter_riverpod: ^3.4.1` 已加（✅）；删 `lib/engine/`、`lib/serial/` 空目录；`main.dart` 换成 `ProviderScope` + `MaterialApp`（`ColorScheme.fromSeed`）+ `NavigationRail`（两个空页面），把现有的 fluent 壳改写过来后从 `pubspec.yaml` 移除 `fluent_ui`。
      *验收*：App 能跑起来，左侧能在「主控 / 设置」间切换，且全项目不再 import `fluent_ui`。
- [ ] **A1 抽 `HelperClient`**：把 `Process.start` / Socket 连接重试 / 按行拆包 / `status` 解析搬进 `lib/ipc/helper_client.dart`；helper 路径改为「exe 同目录优先 + 开发回退」。
      *验收*：主控页按钮经 `HelperClient` 控灯，行为与烟测台一致，代码里再无绝对路径。
- [ ] **A2 状态层**：`helper_state.dart` 定义连接/引擎状态（`disconnected / connecting / ready / running / failed`）Notifier；页面改 `ConsumerWidget`。
      *验收*：状态文案由 Provider 驱动，不再靠页面 `setState` 拼字符串。
- [ ] **A3 主控页**：连接、开始、停止、纯色（几个预设色 + 取色）、关灯，配状ProviderScope态徽标与禁用态。
      *验收*：未连接时控制按钮全禁用；连上后各按钮生效。
- [ ] **A4 设置页骨架**：EMA α 滑条、方案 A/B 切换、COM 口输入框（仅绑 Provider，暂不下发 helper）。
      *验收*：改动即时反映在 UI 上，切页面回来值还在。ProviderScope
- [ ] **A5 JSON 落盘**：`AppConfig` + `fromJson/toJson` + exe 同目录读写；启动时恢复上次配置。
      *验收*：改参数 → 关 App → 重开，值被恢复；手删配置文件不崩。
- [ ] **A6 生命周期收口**：退出发 `quit`、唤醒后重连迁进 Notifier / `onDispose`。
      *验收*：关 App 后任务管理器无残留 `helper.exe`，灯灭，COM 可被再次打开。
- [ ] **A7 UI 打磨（克制）**：间距、禁用态、轻量过渡动效。

### 阶段 B — 配置真正下发 helper

- [ ] **B1 协议草稿**：写清新命令的名字、取值域、越界与非法输入行为（先文档，后 C++）。
- [ ] **B2 helper 侧接收**：`dispatch_line` 加分支 + 参数校验与 clamp；**帧长/节流不接受外部调整**。
- [ ] **B3 引擎侧生效**：配置存 `std::atomic`，热路径只读不加锁（当前 α 写死在 `light_engine.cpp` 的 `alpha = 0.3f`，COM 写死在 `try_serial_ready` 的 `"COM10"`）。
- [ ] **B4 状态上报加宽**：`engine_running` / `com_name` 等回传，前端如实显示。

### 阶段 C — 产品债与收尾

- [ ] **C1 方案 A 确认**：高 Brightness + RGB 跟色的默认通路走通。
- [ ] **C2 方案 B**：luma → Brightness（`01`~`63`）+ 亮度 EMA；RGB 主跟色相；可选 R/G/B 增益或伽马、亮度上限；参数经 JSON + IPC 下发，禁止写死唯一魔法数。
- [ ] **C3 helper 缺口**：父进程 PID 监测、COM 自动重连、DXGI `ACCESS_LOST` 自动恢复、IPC 断连后重回 `listen`（当前 `ipc_run` 只 accept 一次就返回）。
- [ ] **C4 打包与复查**：helper 与 App 同目录分发；全路线红线复查。

---

## 7. 已知产品债与诊断纪律

**亮度模型（阶段 C 才动，A/B 阶段不擅自改）**

- 设备事实：`set_rgb_pc` 的 `{Brightness}` 字段是**全局亮度**（hex ≈ 百分比；`63` ≈ 99%，`01` ≈ 1%）。观感明暗主要跟 Brightness；固定高亮度下改 RGB 灰阶，灯上几乎只有全亮/全黑。降 Brightness 时纯白易偏棕褐（硬件混光妥协）。
- 当前实现：`light_engine.cpp` 的 `send_solid` 与 `produce_colors` 组帧串里 Brightness **写死 `63`**，只对 RGB 做 EMA。
- **诊断纪律**：用户报「EMA α 无效 / 灯硬切亮灭 / 进黑不丝滑」时，**第一句先说明 BRT 仍写死 `63`、明暗主通道尚未接**，勿误判为超时、采样、调度或线程 bug。

**双调色方案（阶段 C 统筹，默认 A）**

| 方案 | 定位 | 要点 |
|------|------|------|
| A — 简单跟色（默认） | 开箱即用 | 高亮度 + RGB 跟屏色；接受亮↔黑较硬切；UI 选项少 |
| B — 暗场/亮度调色（可选） | 进阶用户 | luma → Brightness + 亮度 EMA；极暗可 `01` + 黑；可选校正与亮度上限 |

**其它已知缺口**：无父进程监测；DXGI `ACCESS_LOST` 不自动恢复；COM 名写死 `COM10` 且失败后只能手动 `reconnect`；EMA / 采样 / blur 不可配置；IPC 断连后不重回 listen。

---

## 8. 协作纪律

**节奏**

- 你是学习助手：用户「先学概念 → 再写代码 → 再验收」，边做边学。
- **每次只推进一小步**（一个概念点，或一个函数 / 一处循环的改动）。
- 回复结构固定：**本步目标 → 必要代码或对照 → 一句验收**。回复要短，禁止一次铺开整个阶段。
- 给 Flutter 代码时，简要说明 **Widget 树结构与状态流向**（谁 watch、谁 read、状态存在哪）。
- 给 C++ 核心逻辑时，注释解释**「为什么这么做」**而不只是「做了什么」；热路径避免不必要的内存拷贝。

**工具纪律**

- `user-dart` 的 `analyze_files`（及同类依赖 DTD / Analysis Server 就绪的 MCP）在本机易长时间无响应，会卡住整轮 Agent 对话。
- **禁止**把静态检查默认走该 MCP；改用终端 `dart analyze <paths>`，或编辑器 `ReadLints` / IDE 诊断。
- 其余 `user-dart` 能力（热重载、运行时错误等）可按需用；若同样无响应则立刻改用等价终端命令，不要空等。

**优先级**

1. **前债不可欠**：推进当前项前，若发现更早的实现缺项或与红线不符，先修再继续。
2. **红线问题立刻改**：帧长、节流、互斥、线程退出顺序、stride、IPC 断连、僵尸 helper。
3. **唯一例外**：亮度模型 / 写死 `63` 属用户明确延期的产品债，只在阶段 C 动。

**稳定性排查顺序**

帧长 → 节流（50ms）→ 串口互斥 → 线程退出顺序 → stride → IPC 断连与 helper 退出路径 → COM 是否被残留 helper 占用 → 最后才查采样与调度。
（若现象是「α 无效 / 硬切亮灭」，先查写死的 Brightness，见第 7 节。）

**后置话题**：不主动展开调度算法细节、分页段页、死锁检测算法、磁盘调度、虚拟机；方案 B 的实现细节在阶段 C 之前不主动铺开。
