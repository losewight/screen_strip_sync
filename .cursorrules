# 项目背景
本项目是一个 Windows 平台的 PC 屏幕同步氛围灯驱动（Zeeray 灯带）。
架构：Flutter (Dart) 前端 UI + C++ DLL 后端引擎。两者通过 dart:ffi 零拷贝通信。

# 硬件与通信红线（极其重要）
- 目标设备：米家追光灯带Pro (20 颗物理 RGB 灯珠)。
- 通信协议：纯 ASCII 文本串口，波特率 230400，8N1，必须以 \r\n 结尾。
- 内存溢出规避：单帧指令长度必须严格控制在 110 字节以内（10 段渲染架构，Step=2）。绝不允许为了提高分辨率而随意增加指令长度。

# 技术栈与开发者状态
- 前端 (Flutter)：专注于 UI 交互、JSON 读写和 EMA 时间阻尼算法。不需要任何网络请求和复杂路由。开发者是一个新手，只学习了基础的语法，正在边做边学 Flutter，请在提供 UI 代码时，简要解释 Widget 树结构和状态管理逻辑。
- 后端 (C++)：负责 Windows底层 API (CreateFile 串口)、DXGI 显存抓取与 Stride 内存对齐、物理级降噪 (blurStep)。开发者具备扎实的 C++ 一定算法基础，对内存管理不怎么了解，正在边做边学，C++ 代码请保持极致的性能，避免不必要的内存拷贝。

# 代码生成规范
- C++ 与 Dart 交互的 FFI 结构体，必须明确标注内存对齐方式。
- 给出代码时，如果是业务逻辑核心，请加上注释解释“为什么这么做”而不是仅仅说明“做了什么”。