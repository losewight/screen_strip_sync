# UI 草稿 — Fluent Pro（HTML 原版还原）

来源：`gemini-code-1785231902687.html`  
不接主应用路由 / Riverpod / helper。

系统窗即外壳；内容对齐 HTML 的 `.app-content`（侧栏 + 页面），
不再套一层模拟 `.app-window` / 自绘标题栏。

## 运行

```bash
flutter run -t experiments/ui_draft/main_draft.dart -d windows
```
